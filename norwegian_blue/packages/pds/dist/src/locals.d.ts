import { Response } from 'express';
import pino from 'pino';
import { AuthStore, DidableKey } from '@atproto/auth';
import * as plc from '@atproto/plc';
import { Database } from './db';
import { ServerConfig } from './config';
import ServerAuth from './auth';
import { ServerMailer } from './mailer';
import { App } from '.';
export declare type Locals = {
    logger: pino.Logger;
    db: Database;
    keypair: DidableKey;
    auth: ServerAuth;
    config: ServerConfig;
    mailer: ServerMailer;
};
declare type HasLocals = App | Response;
export declare const logger: (res: HasLocals) => pino.Logger;
export declare const db: (res: HasLocals) => Database;
export declare const keypair: (res: HasLocals) => DidableKey;
export declare const config: (res: HasLocals) => ServerConfig;
export declare const mailer: (res: HasLocals) => ServerMailer;
export declare const auth: (res: HasLocals) => ServerAuth;
export declare const getLocals: (res: HasLocals) => Locals;
export declare const get: (res: HasLocals) => Locals;
export declare const plcClient: (res: Response) => plc.PlcClient;
export declare const getAuthstore: (res: Response, did: string) => AuthStore;
export {};
